import Foundation

/// Stub for this task. Filled in by Task 17 with the logic to bring herdr's
/// host terminal to the foreground after `agent.focus` succeeds.
enum TerminalActivator {
    @MainActor static func activateHerdrHost() {}
}
